#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int m=4;
char h='a';
float n=12.5;
printf("integar=%d ,character=%d ,float=%d",sizeof(m),sizeof(h),sizeof(n));
return (0);
}
